app:
  log_level: INFO
  mode: paper

polymarket:
  api_url: "https://clob.polymarket.com"
  ws_url: "wss://ws-subscriptions-clob.polymarket.com/ws/"

risk:
  max_order_size: 200.0
  max_position_notional: 1000.0
  min_edge_bps: 80

database:
  url: "sqlite+aiosqlite:///./bot.db"

portfolio:
  # merge-note: keep this key for initial capital tracking
  initial_cash_usdt: 20.0

notifications:
  telegram_token: ""
  telegram_chat_id: ""
  # merge-note: keep Feishu key when resolving conflicts
  feishu_webhook_url: ""

llm:
  # merge-note: keep llm.* keys for diagnosis feature
  provider: "openai_compatible"
  base_url: ""
  api_key: ""
  model: ""
